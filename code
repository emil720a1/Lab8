#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector <int>  v={
        {5,4,6,7,8,10},
        };
    reverse(v.begin(),v.end());
    for(int i=0;i<v.size();i++) {
        cout<<v[i]<<" ";
    }
    return 0;
}
